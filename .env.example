# Serve cached fixtures instead of calling live APIs.
# Turn this on before recording a demo: a conference wifi timeout should never
# be the reason a viewer sees a spinner. Leave it off in production so the
# deployed site shows genuinely current detections — /api/targets already falls
# back to the cached snapshot on its own if the live fetch fails.
NEXT_PUBLIC_DEMO_MODE=false
